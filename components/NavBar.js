import { View, Text } from "react-native"
import styles from "../utils/styles"

const NavBar = () => {
    return (<View style={styles.navBar}>
        <Text style={styles.navBarTitle}>Cripto App</Text>
    </View>)
}

export default NavBar